# RETIRED (v1-release plan P10, 2026-07-06) — superseded by ci.yml. This workflow watched
# dead branches (master / modernization-v2, pre-ADR-0001) and could not install the
# git+ssh featurizer pin. Kept as a neutralized stub because repo tooling protects
# .github/ from deletion; `git rm` it at review.
name: retired — old test workflow (disabled)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired — see ci.yml" && exit 1
