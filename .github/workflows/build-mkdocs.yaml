# RETIRED (v1-release plan P10, 2026-07-06) — the mkdocs docs tree was removed in WS3;
# there is no mkdocs.yml. Docs are plain Markdown rendered on GitHub (docs/README.md is
# the index) plus two self-contained HTML artifacts (docs/onboarding.html,
# docs/triage-pg-vs-dssg-triage.html). Kept as a neutralized stub because repo tooling
# protects .github/ from deletion; `git rm` it at review.
name: retired — mkdocs (disabled)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired — see docs/README.md" && exit 1
