*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.cache/
.coverage*
.tox/
docs/site/
**/.hypothesis/

/.install.*.bash.inc
/.python-version
.DS_Store

.ipynb_checkpoints/
venv/
my_db_config.yaml
database.yaml
database*.yaml
dirtyduck/triage/**

*~
**/trained_models/**
**/matrices

*.swp
src/triage/component/postmodeling/postmodeling_tmp.ipynb
src/triage/component/postmodeling/model_class_testing.py
src/triage/component/postmodeling/model_class_testing.ipynb
src/triage/component/postmodeling/model_groups_testing.ipynb

# Local environment and secrets
.env
.secrets
.secrets/

# Session handoff files (Claude-generated, session-local — never committed)
ai-docs/
GIT_REMOTE
TODO.org

# Claude Code hook logs (runtime output, session-local)
logs/

# DirtyDuck local run artifacts + dev config (never committed)
dirtyduck-output/
dirtyduck-database.yaml
dirtyduck/docker-compose.override.yml

# Local model artifacts: `triage run` writes serialized models to the storage root
# (the repo root under the local profile). These are regenerable run output, never source.
*.joblib
# Real KDD Cup 2014 DonorsChoose data (large; not committed — tutorial image ships a subset)
donorschoose/real/
# DonorsChoose dev DB config (baked tutorial creds; never committed — see README to recreate)
donorschoose-database.yaml
# Chicago 311 larger/real export (not committed — tutorial image ships a baked subset) + dev DB config
chicago311/chi311_db/real/
chicago311-database.yaml

# Terraform local state/providers (infra/terraform; the lock file IS committed)
.terraform/
